{
  "extends": ["eslint:recommended", "next/core-web-vitals"],
  "rules": {
    // Add your custom ESLint rules here
    "semi": ["error", "always"], // Example: enforce semi-colons at the end of statements
    "quotes": ["error", "single"] // Example: enforce single quotes for string literals
    // Add more rules as needed
  }
}